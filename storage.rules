rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Wardrobe images: users can read/write only their own folder
    match /wardrobe/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
